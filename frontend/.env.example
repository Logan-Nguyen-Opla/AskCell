# Base URL of the FastAPI backend.
# Leave empty for local dev / public tunnel (Vite proxies /api to port 8000).
# For split deploy (e.g. Vercel + Render), set to your backend URL:
# VITE_API_URL=https://askcell-backend.onrender.com
VITE_API_URL=
